# Hyprland packages — install before starting
# pacman packages: sudo pacman -S <pkg>
# AUR packages:    yay -S <pkg>   (marked with [AUR])

# Core Hyprland
hyprland
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
xdg-utils

# Status bar
waybar

# Wallpaper
hyprpaper

# Launcher (wayland fork of rofi — provides the same `rofi` binary)
rofi-wayland                   # [AUR]

# Clipboard (replaces greenclip)
wl-clipboard
cliphist

# Screenshot (replaces maim/scrot)
grim
slurp
grimblast                      # [AUR]

# Color picker (replaces xcolor)
hyprpicker                     # [AUR]

# Image viewer for wallpaper picker (replaces sxiv)
swayimg

# Idle + screen lock
swayidle
swaylock

# Display config (replaces xrandr)
wlr-randr

# NVIDIA VA-API
libva-nvidia-driver            # [AUR]
egl-wayland

# Notifications
dunst
libnotify

# Polkit agent
lxqt-policykit-agent

# Qt/GTK Wayland support
qt5-wayland
qt6-wayland
